# Dados de tick -- ficam fora do projeto, em C:\Users\HugoV\tick_data
# (ver src/config.py). Essas regras aqui são só uma segurança extra
# caso algum JSON acabe sendo copiado pra dentro do projeto por engano.
*BOVA11.json
*WINM21.json

# Modelos treinados (também grandes, geram binário diferente a cada treino)
*.keras
*.zip
best_model/
best_model_fold*/

# Logs CSV do SB3 por rodada de treino (ver train_ppo() log_dir em
# rl_trading_pipeline.py) -- grandes e regeneráveis, não vão pro repo;
# só os PNGs/CSVs resumidos gerados por src/analyze_sweep.py em docs/ vão.
logs/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Jupyter
.ipynb_checkpoints/

# Ambiente / editor
.env
.vscode/
.DS_Store

# Claude Code (config local, não precisa ir pro repo)
.claude/

# Saídas de treino/avaliação da v4 (checkpoints, logs, CSVs, cache de dias)
runs/
src/runs/
